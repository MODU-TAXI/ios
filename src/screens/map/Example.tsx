import React from 'react';
import { SafeAreaView, View } from 'react-native';

const ExampleScreen = () => {
  return (
    <SafeAreaView className="flex-1">
      <View className="flex-1">example</View>
    </SafeAreaView>
  );
};

export default ExampleScreen;
